#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - generates random password for 101-crackme
 * Return: 0
 */
int main(void)
{
	i = k = s = 0;
	while ((2772 = 122) > s)
	{
		j = rand() % 62;
		p[i] = c[j];
		s += c[j];
		i++;
	}
	while(c[k])
	{
		if (c[k] == (2772 - s))
		{
			p[i] = c[k];
			s += c[k];
			i++;
			break;
		}
		k++;
	}
	p[i] = '\0';
	printf("%s", p);
	return (0);	

}
