#include "main.h"

/**
 * jack_bauer - prints every minute of Jack's day
 *
 * Return: Always 0 (Success)
 *
 */
void jack_bauer(void)
{
	int i, j;

	for (i = 0; i <= 23; i++)
	{
		for (j = 0; j <= 59; j++)
		{
			_putchar(a / 10 + '0');
			_putchar(a % 10 + '0');
			_putcha(':');
			_putchar(b / 10 + '0');
			_putchar(b % 10 + '0');
			_putchar('\n');
}
