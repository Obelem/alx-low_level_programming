#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * Return: Always 0 (Success)
 * @int: The number to be worked on
 */
int _abs(int)
{
	if (int < 0)
		int = int * -1;
		_putchar(int);

	else
		_putchar(int);
}
